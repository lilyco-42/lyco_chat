# 模型在小型设备推理优化 —— 技术要点总结

> 基于 Colibri、BitNet、AWQ/TinyChat、llama.cpp/ggml、MoE 等开源项目的深度分析

---

## 目录

1. [概述：五条核心优化路径](#1-概述五条核心优化路径)
2. [Colibri：面向磁盘的超大规模 MoE 流式推理引擎](#2-colibri面向磁盘的超大规模-moe-流式推理引擎)
3. [BitNet：1-bit / 1.58-bit 极致量化 Transformer](#3-bitnet1-bit--158-bit-极致量化-transformer)
4. [AWQ + TinyChat：激活感知量化 + 边缘推理引擎](#4-awq--tinychat激活感知量化--边缘推理引擎)
5. [llama.cpp / ggml：纯 C/C++ 跨平台量化推理框架](#5-llamacpp--ggml纯-cc-跨平台量化推理框架)
6. [MoE (Mixture of Experts)：稀疏激活降低计算量](#6-moe-mixture-of-experts稀疏激活降低计算量)
7. [关键技术对比总结表](#7-关键技术对比总结表)
8. [综合优化策略](#8-综合优化策略)

---

## 1. 概述：五条核心优化路径

```
┌─────────────────────────────────────────────────────────────────┐
│                    小型设备推理优化全景                          │
├───────────────┬──────────────┬──────────────┬──────────────────┤
│   模型压缩    │   内存管理   │   计算加速   │   架构创新       │
│  (减小体积)   │  (突破瓶颈)  │  (提升吞吐)  │  (原生高效)      │
├───────────────┼──────────────┼──────────────┼──────────────────┤
│ • 1-bit 量化  │ • 磁盘流式   │ • CUDA/Metal │ • MoE 稀疏激活   │
│ • INT4 量化   │ • 层级缓存   │ • SIMD 优化  │ • MLA 压缩注意力 │
│ • AWQ 感知量化│ • KV 压缩    │ • Kernel 融合│ • MQA/GQA 注意力 │
│ • 三元量化    │ • 预取调度   │ • 投机解码   │ • 子层归一化     │
└───────────────┴──────────────┴──────────────┴──────────────────┘
```

---

## 2. Colibri：面向磁盘的超大规模 MoE 流式推理引擎

### 项目概况
- **仓库**: `JustVugg/colibri` | 22.8k Stars
- **语言**: 纯 C，零依赖（无 BLAS，无 Python 运行时）
- **目标**: 在消费级硬件上运行 744B~2.8T 参数的 MoE 模型
- **许可证**: Apache 2.0

### 目录架构
```
c/
├── colibri.c              # 核心主引擎
├── backend_cuda.cu/.h     # CUDA 后端
├── backend_metal.mm/.h    # Metal 后端 (Apple Silicon)
├── backend_vulkan.c/.h    # Vulkan 后端 (AMD/跨平台)
├── backend_gpu_compat.h   # GPU 兼容层 (CUDA ↔ HIP 映射)
├── quant.h                # 量化模块
├── decode_batch.h         # 批量解码
├── kv_persist.h           # KV 缓存持久化
├── route_trace.h          # 路由热力追踪
├── grammar.h              # 结构化输出约束
├── tier.h                 # 分层内存管理
├── uring.h                # io_uring 异步 I/O
├── cfse_pack.c / fse_coli.h / rans.h  # 熵编码压缩
├── tok.h                  # Tokenizer
├── glm.c / olmoe.c / kimi_k3.c / inkling.c  # 各模型引擎
├── tools/                 # 转换/基准工具
└── scripts/               # 辅助脚本
```

### 核心设计哲学

**"将 VRAM、RAM、NVMe 视为统一内存层次结构"**
- 参数不是需要常驻的状态，而是需要按需调度的数据
- 有限快速内存只影响速度，不改变模型语义

### 核心技术机制

#### 2.1 统一内存层次 + 权重 JIT

```
┌──────────┐    常驻 (9.9 GB int4)     ┌──────────┐
│   RAM    │◄─────────────────────────►│ 稠密部分  │ (注意力/共享专家/嵌入)
└────┬─────┘                           │ ~17B 参数 │
     │ 按需流式传输                     └──────────┘
┌────▼─────┐                           ┌──────────┐
│  NVMe    │◄─────────────────────────►│ 路由专家  │ (19,456个专家)
│   SSD    │   370 GB int4 存储         │ 19MB/个   │
└──────────┘                           └──────────┘
```

#### 2.2 学习型 LRU 缓存

- 记录路由历史到 `.coli_usage` 文件
- 自动将热专家固定（pin）在更快内存层
- **使用越久，推理越快**（热缓存自优化）

#### 2.3 I/O 深度优化

| 技术 | 效果 |
|------|------|
| **批量专家联合读取** | 每个 token 的独特专家只读一次 |
| **提前一层预取 (PILOT)** | 路由可预测性 71.6% |
| **异步 I/O 池 (PIPE=1)** | 计算与 I/O 重叠 |
| **O_DIRECT 直接 I/O** | 绕过页缓存，部分 NVMe 提速 +34% |
| **双 SSD 条带化** | 聚合带宽 = 两盘之和 |

#### 2.4 MLA 注意力：KV 状态 57 倍压缩

```
标准 KV Cache: 32,768 floats/token
       ↓  MLA 压缩
压缩 KV Cache: 576 floats/token  (压缩比 57:1)
```

- 支持 KV 状态持久化 (`.coli_kv`)：会话重启零重预填充

#### 2.5 投机解码 (Speculative Decoding)

- 原生 MTP (Multi-Token Prediction) 头
- 单次前向验证 2.2~2.8 个 token
- MTP 头必须 int8（int4 接受率仅 0~4%）
- 语法强制草稿：JSON 约束输出近零成本

#### 2.6 异构执行

| 后端 | 平台 |
|------|------|
| CPU | 纯 C，无依赖 |
| CUDA | NVIDIA GPU (含老旧卡) |
| Metal | Apple Silicon |
| Vulkan | AMD GPU (Mesa/RADV 驱动) |

### 性能基准 (GLM-5.2 int4, 744B 参数)

| 硬件 | 解码速度 | 首 Token 延迟 |
|------|---------|-------------|
| 6× RTX 5090 (完全常驻) | 5.8~6.8 tok/s | ~13s |
| 128GB CPU 桌面 | ~1.8 tok/s (热) | - |
| RTX 5070 Ti 笔记本 | 1.07 tok/s | - |
| 25GB 开发机 | 0.05~0.1 tok/s (冷) | - |

---

## 3. BitNet：1-bit / 1.58-bit 极致量化 Transformer

### 项目概况
- **仓库**: `kyegomez/BitNet` | 1.9k Stars
- **语言**: Python (PyTorch) + CUDA
- **目标**: 用 1-bit 权重替代浮点权重，实现极致压缩
- **许可证**: MIT

### 目录架构
```
bitnet/
├── bitlinear.py                 # ★ 核心：1-bit BitLinear 层
├── bit_linear_new.py            # BitLinear 1.5 (开发中)
├── bit_attention.py             # ★ BitMGQA 注意力机制
├── bit_ffn.py                   # BitFeedForward 前馈网络
├── bit_transformer.py           # ★ BitNetTransformer 完整模型
├── bit_llama.py                 # BitNet + LLaMA 架构
├── bit_mamba.py                 # BitNet + Mamba 架构
├── bit_moe.py                   # ★ BitNet + MoE 混合专家
├── bit_lora.py                  # BitNet + LoRA 微调
├── one_bit_vision_transformers.py # 1-bit 视觉 Transformer
├── inference.py                 # 推理工具
├── replace_hf.py                # HuggingFace 模型替换工具
└── at.py                        # 辅助模块
kernel/                          # CUDA 内核优化
examples/                        # 使用示例
tests/                           # 测试
```

### 核心原理

#### 3.1 BitLinear 数据流

```
输入 x
  │
  ▼
RMSNorm(x)               ← 归一化
  │
  ▼
Absmax Quantization      ← 激活值量化到 INT8 [-127, 127]
  │  scale = 127 / max(|x|)
  │  x_q = round(x * scale) / scale
  ▼
Weight Binarization      ← 权重量化为 ±scale
  │  scale = mean(|w|)
  │  w_bin = sign(w - mean(w)) * scale
  ▼
F.linear(x_q, w_bin)     ← 量化后的矩阵乘法
  │
  ▼
输出 y
```

**关键技巧：STE (Straight-Through Estimator)**
```python
# 前向：用量化值；反向：梯度直通原始值
x_quant = x_norm + (activation_quant(x_norm) - x_norm).detach()
w_quant = w + (weight_quant(w) - w).detach()
```

#### 3.2 BitNet b1.58：三元量化

论文: *"The Era of 1-bit LLMs: All Large Language Models are in 1.58 Bits"*

| 特性 | 说明 |
|------|------|
| **权重值域** | `{-1, 0, 1}` 三种状态 |
| **信息量** | log₂(3) ≈ 1.58 bits/参数 |
| **性能** | 同等规模下与 FP16/BF16 匹配 |
| **优势** | 延迟↓ 内存↓ 吞吐↑ 能耗↓ |

llama.cpp 中实现了两种三元编码格式：
```
TQ1_0: 5个三进制值 → 1字节 (base-3 编码)
TQ2_0: 4个三进制值 → 1字节 (2-bit 直接编码)
```

#### 3.3 BitMoE：量化 + 稀疏激活

```python
class BitMoE(nn.Module):
    def __init__(self, dim, num_experts, top_k=2):
        self.router = NoisyTopkRouter(dim, num_experts, top_k)
        self.experts = [Expert(dim) for _ in range(num_experts)]
        # 每个 Expert = BitLinear + ReLU + BitLinear

    def forward(self, x):
        # 1. 路由器选出 top_k 个专家
        # 2. 仅激活被选中的专家
        # 3. 按 gating score 加权融合输出
```

**Expert 结构**:
```
dim → BitLinear(dim, 4*dim) → ReLU → BitLinear(4*dim, dim) → Dropout
```

#### 3.4 BitMGQA：量化分组查询注意力

```
q_proj: BitLinear(embed_dim, embed_dim)     ← 全维度 Query
k_proj: BitLinear(embed_dim, kv_embed_dim)  ← 压缩 Key (GQA)
v_proj: BitLinear(embed_dim, kv_embed_dim)  ← 压缩 Value
out_proj: BitLinear(kv_embed_dim, embed_dim) ← 输出投影
```

**GQA 优势**: KV 头数 < Q 头数 → 解码速度 ↑ 精度损失小

#### 3.5 llama.cpp 中的 BitNet 推理

```
模型存储：权重矩阵 + 对应的缩放因子 (scale)
推理流程：
  wq + wq_s → build_lora_mm → 动态反量化 → 计算
  wk + wk_s → build_lora_mm → 动态反量化 → 计算
  ...
  ffn_gate + ffn_gate_s → 动态反量化 → SwiGLU
```

---

## 4. AWQ + TinyChat：激活感知量化 + 边缘推理引擎

### 项目概况
- **仓库**: `mit-han-lab/llm-awq` (MIT HAN Lab) | 3.6k Stars
- **奖项**: MLSys 2024 Best Paper Award
- **目标**: 激活感知的 INT3/INT4 权重量化，在边缘设备高效运行
- **许可证**: MIT

### 目录架构
```
awq/
├── kernels/              # CUDA 内核实现
├── quantize/
│   ├── quantizer.py      # ★ 核心量化器 (伪量化 + 真实量化)
│   └── auto_scale.py     # ★ 自动缩放因子搜索
├── utils/                # 工具函数
└── entry.py              # 入口脚本

tinychat/                 # ★ 边缘推理引擎
├── models/               # 模型定义
├── modules/              # 核心推理模块
├── scripts/              # 运行脚本
├── stream_generators/    # 流式生成器
├── utils/                # 工具
├── demo.py               # 演示入口
├── benchmark.py          # 性能基准
└── offline-weight-repacker.py  # 权重重打包
```

### 核心原理

#### 4.1 AWQ 核心思想：激活感知量化

```
传统量化：所有权重通道一视同仁
        ↓
AWQ：识别"显著"通道 → 保护关键通道的量化精度

┌──────────────────────────────────────────────────┐
│  Step 1: 分析激活值分布，找到重要通道             │
│  Step 2: 搜索最佳缩放因子 (20点网格搜索)          │
│  Step 3: 对权重应用缩放 → 量化 → 反量化验证       │
│  Step 4: 选择输出 MSE 最小的缩放因子              │
└──────────────────────────────────────────────────┘
```

#### 4.2 缩放策略

| 策略 | 适用场景 | 操作 |
|------|---------|------|
| `scale_ln_fcs` | LayerNorm → Linear | LN 权重 ÷ scale, Linear 权重 × scale |
| `scale_fc_fc` | Linear → Linear | fc1 ÷ scale, fc2 × scale |
| `scale_gelu_fc` | GELU → Linear | 仅 fc 权重 × scale |

#### 4.3 TinyChat 边缘推理引擎优化

| 优化技术 | 效果 |
|----------|------|
| **W4A16 推理** | 权重 INT4，激活保持 FP16 |
| **自定义 W4A16 CUDA Kernel** | FP16 累加提升吞吐 |
| **注意力 Kernel 融合** | MHA/GQA/MQA + RoPE → 单一 Kernel |
| **KV Cache 预分配** | 避免在线分配开销 |
| **Context Streaming** | 多轮对话复用 KV Cache |
| **Chunk-Prefilling** | TTFT 最高提升 11.45 倍 |
| **Flash Attention** | 可选集成 |
| **模型检查点拆分** | 适配共享内存设备 (Jetson Orin) |
| **混合量化 (VLM)** | Vision: W8A8, LLM: W4A16 |

### 性能数据

| 平台 | 模型 | vs FP16 加速比 |
|------|------|---------------|
| RTX 4090 | LLaMA-3-8B | **2.69x** |
| Jetson Orin | LLaMA-3-8B | **2.95x** (38 tok/s) |
| A100 | LLaMA-3-8B | **1.96x** |

---

## 5. llama.cpp / ggml：纯 C/C++ 跨平台量化推理框架

### 项目概况
- **仓库**: `ggml-org/llama.cpp` | 122.8k Stars
- **语言**: C/C++
- **目标**: 在各类硬件上高效运行 LLM，支持 1.5~8-bit 量化
- **许可证**: MIT

### 目录架构
```
ggml/                          # ★ 底层张量计算库
├── src/
│   ├── ggml.c                 # 核心引擎 (张量操作/内存管理/类型系统)
│   ├── ggml.cpp               # C++ 封装
│   ├── ggml-quants.c/.h       # ★ 量化/反量化实现 (全部格式)
│   ├── ggml-cpu/              # CPU 后端 (AVX/AVX2/AVX512/NEON/RVV)
│   ├── ggml-cuda/             # CUDA 后端
│   ├── ggml-metal/            # Metal 后端
│   ├── ggml-vulkan/           # Vulkan 后端
│   ├── ggml-opencl/           # OpenCL 后端
│   ├── ggml-sycl/             # SYCL 后端 (Intel)
│   ├── ggml-webgpu/           # WebGPU 后端
│   ├── ggml-hip/              # AMD HIP 后端
│   └── ggml-blas/             # BLAS 加速
│
src/                           # 上层模型实现
├── models/                    # ★ 141+ 模型架构实现
│   ├── deepseek.cpp           # DeepSeek-V1 MoE
│   ├── deepseek2.cpp          # DeepSeek-V2/V3 MLA
│   ├── deepseek32.cpp         # DeepSeek-V3.2
│   ├── deepseek4.cpp          # DeepSeek-V4
│   ├── bitnet.cpp             # ★ BitNet b1.58
│   ├── glm4-moe.cpp           # GLM4 MoE
│   ├── qwen2moe.cpp           # Qwen2 MoE
│   ├── mixtral.cpp            # Mixtral MoE
│   ├── cohere2moe.cpp         # Cohere MoE
│   ├── dbrx.cpp               # Databricks MoE
│   ├── ernie4-5-moe.cpp       # Ernie MoE
│   └── ...
├── llama.cpp                  # 主推理循环
├── llama-model.cpp            # 模型加载
├── llama-context.cpp          # 推理上下文
├── llama-kv-cache.cpp         # KV 缓存管理
├── llama-quant.cpp            # 量化工具
└── llama-mmap.cpp             # 内存映射
```

### 核心量化格式体系

#### 5.1 完整量化格式矩阵

| 格式 | 位宽 | 块大小 | 原理 | 特点 |
|------|------|--------|------|------|
| **Q1_0** | 1-bit | 32 | 仅符号位 | 极限压缩 |
| **Q2_0** | 2-bit | 32 | {-1,0,1,2}×scale | 高压缩 |
| **Q3_K** | 3-bit | 256 | 超级块 16 子块 | 平衡 |
| **Q4_0** | 4-bit | 32 | 对称 [-8,7]×scale | 经典 |
| **Q4_1** | 4-bit | 32 | 非对称含 min | 精度好 |
| **Q4_K** | 4-bit | 256 | 超级块 32 子块 | 推荐 |
| **Q5_0** | 5-bit | 32 | 对称 + 高位分离 | 精度优 |
| **Q5_1** | 5-bit | 32 | 非对称含 min | 精度优 |
| **Q5_K** | 5-bit | 256 | 超级块 | 高精度 |
| **Q6_K** | 6-bit | 256 | 超级块 | 近无损 |
| **Q8_0** | 8-bit | 32 | 对称 [-127,127] | 参考级 |
| **IQ1_S** | 1.56 | - | 1-bit+delta | 超极限 |
| **IQ1_M** | 1.75 | - | 混合 delta | 超极限 |
| **IQ2_XXS** | 2.06 | - | 256 查找表 | 最优 2-bit |
| **IQ2_XS** | 2.31 | - | 512 查找表 | 优秀 2-bit |
| **IQ3_XXS** | 3.06 | - | 3-bit 网格查找 | 最优 3-bit |
| **IQ4_NL** | 4-bit | - | 非线性查找表 | 高质量 4-bit |
| **IQ4_XS** | 4-bit | - | 非线性+子块scale | 高质量 4-bit |
| **TQ1_0** | 1.58 | - | 三元 {-1,0,1} base-3 | BitNet |
| **TQ2_0** | 1.58 | - | 三元 {-1,0,1} 2-bit | BitNet |
| **MXFP4** | 4-bit | - | 共享指数 e8m0 | 浮点量化 |
| **NVFP4** | 4-bit | - | NVIDIA ue4m3 scale | 浮点量化 |

#### 5.2 K-quant 超级块量化原理

```
超级块 (256 元素)
  │
  ├─ 16 或 32 个子块，每块独立 scale/min
  │
  └─ 子块的 scale/min 再以 6-bit 量化存储

两级量化 = 高精度 + 低存储
```

#### 5.3 IQ (Importance-aware Quantization) 原理

```
预定义 8 维网格点 → 每 8 元素匹配最近点 → 存网格索引 + 符号

优势: 适配权重的非均匀分布，比均匀量化精度更高
```

---

## 6. MoE (Mixture of Experts)：稀疏激活降低计算量

### 核心思想

```
Dense FFN (传统):
  所有 token × 全部参数 → 计算量 O(n_ff)

MoE FFN:
  每个 token × Top-K 专家 → 计算量 O(K × n_ff_exp), K << n_expert
```

### 6.1 架构演进

```
第一代: Switch Transformer (Google)
  → 每个 token 选 1 个专家

第二代: Mixtral / DeepSeek-V1
  → 每个 token 选 Top-K 专家 (K=2 或 8)
  → 加入共享专家 (始终激活)

第三代: DeepSeek-V2/V3
  → 细粒度专家分割
  → MLA 压缩 KV Cache
  → 辅助损失无关的负载均衡
  → MTP 多 Token 预测
```

### 6.2 DeepSeek-MoE 在 llama.cpp 中的实现

```
Layer 0 ~ n_layer_dense_lead-1:
  [Attention + Dense FFN]     ← 浅层用标准 FFN

Layer n_layer_dense_lead ~ end:
  [Attention + MoE(路由专家) + Shared Expert]
    ├── 路由器: Softmax(W_gate · x)
    ├── 选 Top-K 专家，仅激活 K 个
    └── 共享专家: 始终激活，捕获通用知识
```

**关键优化**:
- **分组张量存储**: 专家权重按 `[n_expert]` 维度组织，支持批量矩阵运算
- **共享专家**: 额外的 FFN 始终激活，减少专家间知识冗余
- **Dense 前导层**: 浅层特征提取不需要稀疏专家

### 6.3 MLA (Multi-head Latent Attention) —— KV 压缩

```
标准 MHA:
  Q, K, V 全部完整维度 → KV Cache 巨大

MLA:
  KV 投影到低秩空间 (kv_lora_rank)
  Q 吸收 K 的权重矩阵 (wk_b)
  V 在 softmax 后再上采样 (wv_b)

KV Cache = kv_lora_rank + n_embd_head_qk_rope (远小于标准 MHA)
```

```
┌─────────────────────────────────────────────┐
│  MLA 前向流程                                │
│                                              │
│  Input → wkv_a_mqa → [kv_cmpr, k_pe]        │
│                     ↓                        │
│              kv_cmpr (低秩压缩)               │
│                     ↓                        │
│          缓存到 KV Cache (极小)               │
│                                              │
│  Q: wq_a → RMSNorm → wq_b                   │
│       ↓                                      │
│  q_nope × wk_b → q_nope_absorbed            │
│       ↓                                      │
│  Q = [q_nope_absorbed, q_pe]                │
│  K = [kv_cmpr, k_pe]                        │
│  V = kv_cmpr                                 │
│       ↓                                      │
│  Attention → × wv_b (V 上采样)               │
└─────────────────────────────────────────────┘
```

### 6.4 MoE 在小型设备推理的优化要点

| 优化方向 | 技术 | 效果 |
|----------|------|------|
| **稀疏激活** | Top-K 路由 | 只计算 ~5% 的专家 |
| **专家剪枝** | 移除不重要专家 | 模型体积 ↓ |
| **知识蒸馏** | 大 MoE → 小 Dense | 保留能力，减少参数 |
| **共享专家** | 通用知识不重复存 | 减少专家冗余 |
| **动态加载** | Colibri 流式方案 | 超大模型跑在单机上 |
| **量化 + MoE** | BitMoE (BitNet) | 双重压缩 |

---

## 7. 关键技术对比总结表

### 7.1 量化方案对比

| 方案 | 位宽 | 精度损失 | 压缩比 | 适用场景 |
|------|------|---------|--------|---------|
| **BitNet b1.58** | 1.58-bit | 小（需从头训练） | ~10x | 定制硬件 |
| **AWQ INT4** | 4-bit | 极小 | ~4x | 边缘 GPU |
| **Q4_K_M** | 4.5-bit | 极小 | ~3.5x | 通用推荐 |
| **Q5_K_M** | 5.5-bit | 几乎无损 | ~3x | 高质量 |
| **IQ2_XXS** | 2.06-bit | 可控 | ~7.5x | 极限压缩 |
| **TQ1_0** | 1.58-bit | 小（后训练） | ~10x | BitNet 推理 |

### 7.2 推理引擎对比

| 引擎 | 语言 | 后端 | MoE 支持 | 特色 |
|------|------|------|---------|------|
| **Colibri** | C | CPU/CUDA/Metal/Vulkan | ★ 原生 | 磁盘流式, 统一内存 |
| **llama.cpp** | C/C++ | 全平台 | ★ 141+ 模型 | 生态最全 |
| **TinyChat** | Python | CUDA | 有限 | Kernel 融合最优 |
| **BitNet** | PyTorch | CUDA | ★ BitMoE | 1-bit 训练 |

### 7.3 内存优化技术对比

| 技术 | 适用场景 | 效果 |
|------|---------|------|
| **KV Cache 量化** | 所有 LLM | KV 缓存减少 2-8x |
| **MLA 压缩** | DeepSeek 系 | KV 压缩 57x |
| **GQA/MQA** | 解码器 | KV 头数减少 4-8x |
| **磁盘流式** | 超大 MoE | 突破 RAM 限制 |
| **分层缓存** | Colibri | 热专家常驻, 冷专家按需 |
| **投机解码** | 内存充足时 | 吞吐提升 2-2.8x |

---

## 8. 综合优化策略

### 8.1 不同设备的推荐方案

```
┌─────────────────────────────────────────────────────────┐
│  手机/嵌入式 (4-8GB)                                     │
│  ├── llama.cpp Q4_K_M + GQA 模型                        │
│  ├── IQ2_XXS 极限压缩                                    │
│  └── MoE 小模型 + 专家剪枝                               │
├─────────────────────────────────────────────────────────┤
│  笔记本/台式机 (16-32GB)                                 │
│  ├── llama.cpp Q5_K_M + MoE 模型                        │
│  ├── TinyChat AWQ INT4 + CUDA                           │
│  └── Colibri 磁盘流式 (NVMe)                             │
├─────────────────────────────────────────────────────────┤
│  边缘服务器 (Jetson Orin 64GB)                           │
│  ├── TinyChat AWQ INT4 → 38 tok/s                       │
│  ├── Flash Attention 集成                                │
│  └── Context Streaming 复用 KV Cache                    │
├─────────────────────────────────────────────────────────┤
│  桌面工作站 (RTX 4090 + NVMe)                            │
│  ├── Colibri 流式超大 MoE                                │
│  ├── 双 SSD 条带化                                       │
│  └── CUDA + Metal/Vulkan 混合                            │
└─────────────────────────────────────────────────────────┘
```

### 8.2 组合优化策略

```
Step 1: 模型架构选择
  ├── 选择 MoE 架构 (稀疏激活, 降低计算量)
  ├── 使用 GQA/MQA 注意力 (减少 KV Cache)
  └── 使用 MLA 注意力 (极限 KV 压缩, DeepSeek 系)

Step 2: 量化压缩
  ├── 训练阶段: BitNet b1.58 (1.58-bit 从头训练)
  ├── 后训练: AWQ (激活感知 INT4)
  └── 推理: llama.cpp Q4_K_M / IQ2_XXS

Step 3: 内存/存储优化
  ├── KV Cache 量化 + 预分配
  ├── 磁盘流式加载 (Colibri 方案, 超大模型)
  └── 学习型热缓存 (越用越快)

Step 4: 计算加速
  ├── 投机解码 (MTP 头, 2-3x 吞吐)
  ├── Kernel 融合 (注意力 + RoPE)
  ├── 异步 I/O 预取 (计算与加载重叠)
  └── SIMD/张量核心加速 (AVX512/NEON/CUDA)
```

### 8.3 关键取舍

| 权衡维度 | 选择 |
|----------|------|
| **精度 vs 体积** | Q5_K_M 是性价比最佳点 |
| **速度 vs 内存** | 投机解码快但需额外内存 |
| **通用性 vs 效率** | llama.cpp 通用, Colibri/TinyChat 更极致 |
| **训练成本 vs 推理成本** | AWQ 后训练量化成本低, BitNet 需重新训练 |
| **MoE 规模 vs 设备限制** | 磁盘流式让设备限制不再是硬边界 |

---

> **总结**: 小型设备推理优化的本质是在 **精度-内存-速度** 三维空间中寻找最优平衡点。当前最前沿的方案是 **MoE 稀疏激活 + 极致量化 (1.58-bit) + 磁盘流式 + 投机解码** 的组合，让千亿参数模型在消费级硬件上运行成为现实。
